# FastAPI core
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# Supabase
supabase==2.4.2

# AI APIs
openai==1.30.1
anthropic==0.40.0
google-generativeai==0.6.0

# Background tasks
celery==5.4.0
redis==5.0.4

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP client (async)
httpx==0.27.0

# Audio processing
pydub==0.25.1

# Utilities
orjson==3.10.3

# Document export (DOCX)
python-docx==1.1.2

# PDF page counting (for page-based document limit)
pypdf==4.2.0

# Server-side PDF generation (pure Python, embeds TTF fonts — no system deps)
fpdf2==2.7.9
# Text shaping — required so Thai vowel/tone marks position correctly above
# their base consonant. Without this, fpdf2 places each Unicode codepoint at
# a fixed advance width and combining marks float as separate glyphs that
# overlap neighboring characters (visible as "characters touching each other"
# in titles). uharfbuzz is fpdf2's optional HarfBuzz binding — when present,
# pdf.set_text_shaping(True) enables proper script shaping for Thai / Arabic
# / Devanagari etc.
uharfbuzz==0.39.3
beautifulsoup4==4.12.3

# Image resizing before sending to Claude — Anthropic enforces ≤2000px per
# dimension when many images are in one request (Error 400 invalid_request_error).
Pillow==10.4.0
